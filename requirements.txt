# --- Runtime dependencies for fakenews_detector ---
# Pinning conservatively to versions that are compatible with each other
# on Python 3.12. Bump as needed.

# Configuration
python-dotenv>=1.0.0

# HTTP + scraping
requests>=2.31.0
beautifulsoup4>=4.12.0

# Data + classical ML
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0
joblib>=1.3.0

# NLP
nltk>=3.8.1
pyspellchecker>=0.8.0

# Transformers / embeddings (heavy)
torch>=2.2.0
transformers>=4.40.0
sentence-transformers>=2.7.0

# LLM clients for the final web-presence gate. Either is sufficient -
# the validator auto-picks based on which API key is set in .env.
# Gemini is recommended (more generous free tier).
google-genai>=0.3.0
anthropic>=0.25.0

# Web UI (optional - only needed if you run the webapp/ Flask app)
flask>=3.0.0
waitress>=3.0.0
